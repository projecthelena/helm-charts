apiVersion: v1
kind: Secret
metadata:
  name: {{ include "clustercost-dashboard.fullname" . }}-config
  labels:
    {{- include "clustercost-dashboard.labels" . | nindent 4 }}
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- $config := deepCopy .Values.config }}
{{- $_ := set $config "defaultAgentToken" (include "clustercost-dashboard.defaultAgentToken" .) }}
{{- if hasKey .Values "agents" }}
{{- $_ := set $config "agents" .Values.agents }}
{{- end }}
stringData:
  config.yaml: |
    {{- toYaml $config | nindent 4 }}
